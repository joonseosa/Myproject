<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sol.admin.dao.MemberDAO">

	<delete id="memberDelete" parameterType="mdto">
		delete from member
		where mem_id=#{mem_id}
<!-- 		<foreach collection="list" item="mem_id" open="(" close=")" -->
<!-- 			separator=","> -->
<!-- 			where mem_id=#{mem_id} -->
<!-- 		</foreach> -->
	</delete>
</mapper>